print("Welcome to the tip calculatur.")
bill=input("What was the total bill? ")

percentage=input("What percentage tip would you like to give? 10,12 or 15 " )

people=input("How many people to split the bill? ")

result=float(percentage)/100*float(bill)/float(people)
print(f"Tip per person is {round(result,2)} Rs.")

final_result=float(bill)/float(people)+float(result)

print(f"Each person should pay {final_result} Rs.")
